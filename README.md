# DSA_SEARCH_PROJECT

This project was created for my Data Structures and Algorithms course.  
The program compares **Sequential Search** and **Binary Search** on a sorted array of integers.

## 📌 Features

- Uses a hardcoded sorted array of integers  
- Menu with options:
  - Sequential Search
  - Binary Search
  - Exit
- Asks the user for a target value
- Displays:
  - Whether the number was found or not
  - The index of the number (or -1 if not found)
  - The number of comparisons made

